version: '3.9'

services:
  app:
    build: .
    ports:
      - "8080:8000"   # host:container
    volumes:
      - .:/var/www/html
    environment:
      - APP_ENV=local
      - APP_DEBUG=true
    command: php artisan serve --host=0.0.0.0 --port=8000
